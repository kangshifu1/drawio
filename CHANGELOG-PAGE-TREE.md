# 页面树增强版更新日志

## v31.4.5-page-tree.4 — 2026-09-25（页面树删除，预发布）

- 左侧页面树增加“删除页面”按钮和确认对话框。
- 删除父页时保留子页，将直接子页移至顶级，清除其失效父级引用。
- 删除及子页层级调整合并为一个可撤销操作；支持重做。
- 防止删除最后一个页面，增加只读及确认后状态变化保护。
- 包含此前的桌面关闭、保存和打包隐私修复。


## v31.4.5-page-tree.3 — 2026-09-23（Windows 关闭修复，预发布）

- 修复桌面模块尚未加载时提前创建编辑器，导致关闭消息无人响应的启动时序缺陷。
- 等待桌面覆盖逻辑和所需脚本加载完成后再启动，保留原生未保存确认流程。
- 添加加载顺序回归测试及已保存文件的原生窗口关闭测试。
- 本次发布 Windows x64 安装包；共享逻辑已在 Mac 打包应用验证保存及关闭，尚无 Windows 实机验证。


## 安装包隐私清理 — 2026-09-19

- 排除开发测试脚本、本机路径所在的构建笔记、日志、环境文件和历史构建产物。
- 重新打包第 1、2 版 Windows 附件并更新校验值；功能保持各原版本一致。
- 增加 Mac / Windows 打包排除规则检查。


## v31.4.5-page-tree.2 — 2026-09-19（保存修复，预发布）

- 修复应用改名后未被识别为 Electron 桌面版，导致打开本地文件后每次保存进入网页保存流程的问题。
- 同步调整导出和 VSDX 导入入口的桌面环境识别。
- 增加 12 项环境识别测试，以及真实安装包打开临时文件、连续两次写回同一路径的回归检查。
- 重新构建 Mac arm64、Mac Intel x64 和 Windows x64 安装包；请从新的修复版本下载。


## v31.4.5-page-tree.1 — 2026-09-15（预发布）

基于 Draw.io 31.4.5，首个页面树增强版。发布标签标识定制迭代；桌面应用的基础版本号仍为 31.4.5。

### 新增

- 工具栏最前面的页面树入口，独立左侧面板，支持关闭按钮和 Esc 收起。
- 多级父子页面、展开／折叠、搜索和页面定位。
- 拖拽与下拉选择调整父级，防止自引用及循环关系。
- 新增页面、子页面、重命名，沿用原生页面切换及撤销／重做。
- 使用稳定页面 ID 将父级关系保存到 `diagram` 的 `sheet-parent` 属性。
- 删除父页后子页仍然可访问，撤销删除可恢复关系。
- Mac arm64、Mac Intel x64、Windows x64 NSIS 个人安装包。
- 独立桌面名称、图标、应用 ID，关闭官方自动更新。

### 验证

- 页面树 3 项 Node 测试通过。
- 真实 Chromium 验证搜索、导航、拖拽、保存重开、撤销重做、父页删除、只读保护和 1090×678 面板布局。
- 桌面上游 158 项测试通过。
- Mac arm64 应用启动和页面树操作验证通过；Mac x64 包在 Apple 芯片 Mac 的 Rosetta 环境下验证通过。
- 两个 DMG 的镜像校验、两个 Mac 应用的临时签名完整性、Windows 安装包压缩数据完整性通过。

### 已知限制

- Windows 尚未实机安装测试；Mac x64 尚未 Intel 实机验证。
- 未进行开发者证书签名／公证，安装时可能出现系统提示。
- 暂无跨文件工作区、纯目录节点或层级协作同步。
- 仅验证了本文说明的功能与环境，并未完成所有 Draw.io 主题及所有功能的回归。
