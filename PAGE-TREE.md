# 本地 Draw.io 页面树

基于官方 https://github.com/jgraph/drawio 源码，起始提交 `f3abfe0`。

## 运行

在仓库根目录运行：

```sh
python3 -m http.server 8765 --bind 127.0.0.1 --directory src/main/webapp
```

浏览器访问：

http://127.0.0.1:8765/?ui=kennedy&lang=zh&mode=device&splash=0

通过“文件 → 从以下位置打开 → 设备”打开 `.drawio` 文件。点击工具栏最左侧的“项目页面”树形图标，在最左侧展开独立面板。元素选择区在其右侧。

## 使用

- 页面面板默认收起；点击工具栏首位图标可展开／收起，也可用面板右上角 × 或 Esc 关闭。
- 点击树中的页面名称切换 sheet；搜索会同时显示匹配页面的祖先。
- 点击箭头展开或折叠；“展开全部”恢复所有节点。
- 选择页面后，用“当前页的上级”设置父页面。选“顶级页面”移出当前父级。
- 拖动页面到另一页面，可将前者设为后者的子页。
- “+ 子页面”在当前页下新建页面；“重命名”沿用原生对话框。
- 父级调整和新增子页支持原生撤销／重做。保存文件后层级一起保存。
- 删除父页面不会删除子页；子页显示为顶级。撤销删除恢复关联。
- 原有文件首次打开时显示为平铺列表，需要手动设置一次父子关系。
- 树的同级排序沿用底部标签顺序。

## 验证

```sh
node --test tests/sheet-tree.test.js
node tests/sheet-tree.browser.cjs
```

浏览器测试需启动上述本地服务，并安装 Playwright 和 Chromium（本次环境已具备）。测试使用合成文件，不修改用户图纸。

## 交付范围

提供本地网页版源码及 Mac（Apple 芯片 / Intel）、Windows x64 个人构建安装包，下载见 [Releases](https://github.com/kangshifu1/drawio/releases)。桌面构建说明见 [packaging/README.md](packaging/README.md)。父子结构限于单个文件内，暂不包含独立文件夹、跨文件项目管理或层级协作同步。通过其他编辑器再次保存时，自定义属性是否保留取决于该编辑器行为。

## 页面树删除

选中左侧页面树中的页面，点击面板底部“删除页面”，确认后删除该页及其图形内容。直接子页面移至顶级，其下级结构保留。可通过撤销恢复页面及原层级。只读状态和仅剩一个页面时禁止删除。
